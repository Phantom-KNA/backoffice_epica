apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webappoperacion
  namespace: alquimia
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
    appgw.ingress.kubernetes.io/override-frontend-port: "8090"
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          service:
            name: webappoperacion
            port:
              number: 80
        pathType: Exact